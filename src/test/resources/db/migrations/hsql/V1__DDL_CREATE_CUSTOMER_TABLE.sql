CREATE SEQUENCE CUSTOMERS_ID_SEQ AS BIGINT START WITH 1 INCREMENT BY 1;

CREATE TABLE CUSTOMERS (
  ID BIGINT GENERATED BY DEFAULT AS SEQUENCE CUSTOMERS_ID_SEQ PRIMARY KEY,
  NAME VARCHAR(100) NOT NULL,
  DOCUMENT VARCHAR(100) NOT NULL,
  EMAIL VARCHAR(100) NOT NULL,
  BALANCE DOUBLE NOT NULL
);